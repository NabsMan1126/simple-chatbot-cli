# simple-chatbot-cli
A Python rule-based command-line chatbot that responds to user input using predefined responses.
## Description

This is a simple rule-based chatbot built with Python that responds to user input using predefined replies. It uses basic string matching to recognize keywords and generate appropriate responses.

The project demonstrates fundamental programming concepts such as conditional logic, string handling, loops, and building an interactive command-line application.
